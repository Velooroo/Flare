# TLS Certificates (optional)
# If not set, flared will generate self-signed cert
FLARE_TLS_CERT=/path/to/cert.pem
FLARE_TLS_KEY=/path/to/key.pem

# Or use Let's Encrypt
# FLARE_TLS_CERT=/etc/letsencrypt/live/yourdomain/fullchain.pem
# FLARE_TLS_KEY=/etc/letsencrypt/live/yourdomain/privkey.pem

# Apps directory (default: ~/.flare/apps)
FLARE_APPS_DIR=/home/pi/.flare/apps

# Forge authentication
FLARE_USER=your-username
FLARE_PASS=your-token-or-password

# Daemon port (default: 7530)
FLARE_PORT=7530

# Gateway port (default: 80)
FLARE_GATEWAY_PORT=80

# Log level (error, warn, info, debug, trace)
RUST_LOG=info
